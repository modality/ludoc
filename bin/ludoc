#!/usr/bin/env ruby

begin
  require 'ludoc'
rescue LoadError
  require 'rubygems'
  require 'bundler/setup'
  require 'ludoc'
end

args = Hash[ ARGV.flat_map{|s| s.scan(/--?([^=\s]+)(?:=(\S+))?/) } ]
ludoc = Ludoc::Base.new args
ludoc.render
